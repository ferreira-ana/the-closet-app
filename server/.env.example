# ----- Server -----
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development
PORT=3000

# ----- Database (MongoDB) -----
# Option A: Use a single connection string with a <PASSWORD> placeholder.
# Replace <username> and <dbname>, and keep <PASSWORD> literally in the string.
# Then set DATABASE_PASSWORD below.
DATABASE=mongodb+srv://<username>:<PASSWORD>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
DATABASE_PASSWORD=yourPassword

# Option B: If you prefer not to use a placeholder, comment the two lines above and set:
# DATABASE=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@your-cluster.mongodb.net/<dbname>?retryWrites=true&w=majority
# (Leave DATABASE_PASSWORD unset in that case.)

# ----- JWT (Access & Refresh) -----
# Generate high-entropy secrets (see commands below). No fixed length required,
# but at least 32 bytes (≈64 hex chars) is strongly recommended.
JWT_SECRET=replace-with-hex-or-base64-secret
JWT_EXPIRES_IN=90d

JWT_REFRESH_SECRET=replace-with-hex-or-base64-refresh-secret
JWT_REFRESH_EXPIRES_IN=7d

# Cookie for refresh token (number of days)
JWT_REFRESH_COOKIE_EXPIRES_IN=7
